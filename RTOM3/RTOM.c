#include "RTOM.h"

// rmat[9] and launched are accessible, more variables can be added as needed
// rmat is the direction cosine matrix elements
// launched indicates launch detection, it is equal to 0 prior to launch, 1 after launch
// launch can be simulated by pulling either SCL or SDA pins to ground

void rtom_init(void)
// initialization code which gets called once right after power up
{
	
}

void rtom(void)
// rtom code gets called 40 times per second
{
	
}
